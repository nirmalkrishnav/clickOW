<Window x:Class="ClickOw.Settings.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:ClickOw.Settings"
        Title="ClickOw Settings"
        Width="440"
        Height="640"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanMinimize"
        Background="#FF1E1E1E"
        Foreground="#FFEAEAEA"
        FontFamily="Segoe UI"
        FontSize="13">
    <Window.Resources>
        <local:StringToBrushConverter x:Key="BrushConverter" />
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="#FFEAEAEA" />
            <Setter Property="VerticalAlignment" Value="Center" />
        </Style>
        <Style TargetType="CheckBox">
            <Setter Property="Foreground" Value="#FFEAEAEA" />
            <Setter Property="Margin" Value="0,6" />
        </Style>
        <Style x:Key="Header" TargetType="TextBlock">
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="FontSize" Value="14" />
            <Setter Property="Margin" Value="0,14,0,6" />
            <Setter Property="Foreground" Value="#FF4FC3F7" />
        </Style>
    </Window.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="18">
        <StackPanel>
            <TextBlock Text="General" Style="{StaticResource Header}" />
            <CheckBox Content="Enable ClickOw (Ctrl+Alt+C)" IsChecked="{Binding Enabled}" />
            <CheckBox Content="Laser pointer mode (Ctrl+Alt+L)" IsChecked="{Binding LaserMode}" />
            <CheckBox x:Name="StartupCheck" Content="Run at Windows startup" IsChecked="{Binding RunAtStartup}" />

            <TextBlock Text="Colors (ARGB hex, e.g. #FF3DA5FF)" Style="{StaticResource Header}" />
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="120" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="28" />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Grid.Column="0" Text="Press" />
                <TextBox Grid.Row="0" Grid.Column="1" Margin="4" Text="{Binding PressColor, UpdateSourceTrigger=PropertyChanged}" />
                <Border Grid.Row="0" Grid.Column="2" Margin="4" Background="{Binding PressColor, Converter={StaticResource BrushConverter}}" />

                <TextBlock Grid.Row="1" Grid.Column="0" Text="Release" />
                <TextBox Grid.Row="1" Grid.Column="1" Margin="4" Text="{Binding ReleaseColor, UpdateSourceTrigger=PropertyChanged}" />
                <Border Grid.Row="1" Grid.Column="2" Margin="4" Background="{Binding ReleaseColor, Converter={StaticResource BrushConverter}}" />

                <TextBlock Grid.Row="2" Grid.Column="0" Text="Right-click" />
                <TextBox Grid.Row="2" Grid.Column="1" Margin="4" Text="{Binding RightColor, UpdateSourceTrigger=PropertyChanged}" />
                <Border Grid.Row="2" Grid.Column="2" Margin="4" Background="{Binding RightColor, Converter={StaticResource BrushConverter}}" />

                <TextBlock Grid.Row="3" Grid.Column="0" Text="Drag" />
                <TextBox Grid.Row="3" Grid.Column="1" Margin="4" Text="{Binding DragColor, UpdateSourceTrigger=PropertyChanged}" />
                <Border Grid.Row="3" Grid.Column="2" Margin="4" Background="{Binding DragColor, Converter={StaticResource BrushConverter}}" />

                <TextBlock Grid.Row="4" Grid.Column="0" Text="Laser" />
                <TextBox Grid.Row="4" Grid.Column="1" Margin="4" Text="{Binding LaserColor, UpdateSourceTrigger=PropertyChanged}" />
                <Border Grid.Row="4" Grid.Column="2" Margin="4" Background="{Binding LaserColor, Converter={StaticResource BrushConverter}}" />
            </Grid>

            <TextBlock Text="Sizing &amp; timing" Style="{StaticResource Header}" />
            <TextBlock Text="Click size" Margin="0,4,0,0" />
            <Slider Minimum="16" Maximum="120" Value="{Binding ClickSize}" TickFrequency="4" IsSnapToTickEnabled="True" />
            <TextBlock Text="Effect duration (ms)" Margin="0,4,0,0" />
            <Slider Minimum="150" Maximum="1500" Value="{Binding EffectDuration}" TickFrequency="50" IsSnapToTickEnabled="True" />
            <TextBlock Text="Drag threshold (px)" Margin="0,4,0,0" />
            <Slider Minimum="1" Maximum="30" Value="{Binding DragThreshold}" TickFrequency="1" IsSnapToTickEnabled="True" />
            <TextBlock Text="Laser thickness" Margin="0,4,0,0" />
            <Slider Minimum="1" Maximum="20" Value="{Binding LaserThickness}" TickFrequency="1" IsSnapToTickEnabled="True" />
            <TextBlock Text="Laser fade (ms)" Margin="0,4,0,0" />
            <Slider Minimum="200" Maximum="2000" Value="{Binding LaserFadeDuration}" TickFrequency="100" IsSnapToTickEnabled="True" />

            <Button x:Name="CloseButton" Content="Close" Width="90" Height="30" HorizontalAlignment="Right"
                    Margin="0,20,0,0" Click="CloseButton_Click" />
        </StackPanel>
    </ScrollViewer>
</Window>
